<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Two-Factor Authentication Setup</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1>Set Up Two-Factor Authentication</h1>
        <div class="card mt-3">
            <div class="card-body">
                <% if (qrCodeUrl) { %>
                    <img id="qr-code" src="<%= qrCodeUrl %>" alt="QR Code" class="mb-3" />
                <% } %>
                <form action="/verify-2fa" method="POST">
                    <div class="form-group">
                        <label for="token">Enter the token from your authenticator app:</label>
                        <input type="text" id="token" name="token" class="form-control" placeholder="Enter token" required>
                    </div>
                    <button type="submit" class="btn btn-primary">Verify Token</button>
                </form>
                <% if (verificationStatus) { %>
                    <p id="verification-status" class="mt-3"><%= verificationStatus %></p>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>



